apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# If you create the namespace elsewhere, you can omit this next block
# and make sure that namespace exists before this Kustomization runs.
resources:
  - helmrepo.yaml       # HelmRepository (namespace: flux-system)
  - helmrelease.yaml    # HelmRelease
  - clusterissuer.yaml  # CRs that depend on the chart
  - issuers.yaml
# Or whatever the file is named (values.yaml via valuesFrom is fine too)
