apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-config
  namespace: plex
spec:
  accessModes: [ "ReadWriteOnce" ]
  resources: { requests: { storage: 30Gi } }
  storageClassName: longhorn-2rep
